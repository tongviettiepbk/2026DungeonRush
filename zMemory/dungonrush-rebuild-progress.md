---
name: dungonrush-rebuild-progress
description: "Tiến độ rebuild DungOnRush theo kiến trúc StickIdle — Data/Gears/Companion/Map/Combat-scaffold/Patterns/Localize/BaseUnit+BattleMechanic foundation xong"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2353e537-b729-4ea6-a1ea-1d48264d4ff5
  modified: 2026-07-29T17:05:56.969Z
---

Rebuild DungOnRush theo kiến trúc [[stickidle-architecture]], làm từng phần lấy core base (chưa cần đúng data gốc). Project đích: `E:\00Work\00Project\2026DungOnRush\2026DungeonRushUnity` (Unity 6000.3.9f1, editor ở `E:\Installed\Unity\6000.3.9f1`). Code ở `Assets/_Assets/Scripts`, roadmap trong `Assets/_Assets/Scripts/README.md`.

Tiến độ (2026-07-24): **Phần 2 Data ✅** — GameData/StaticGameData/UserData + BaseUserData, 4 module mẫu (Profile, Campaign, Items, Settings), StaticItemData (ScriptableObject/Resources), GameDataTester ở `_Test/`. Đã thêm Newtonsoft vào manifest. Compile-check bằng dotnet + Unity dlls OK.

**C1 Vũ khí ✅ (data thật)** — module `Scripts/Weapons/`: enum `Rarity` (10 bậc, ở Common/GameEnums.cs) + `WeaponType`(Melee/Range), `WeaponData:ScriptableObject` (assetName khoá duy nhất vì itemId trùng; fields: itemId, displayName, rarity, weaponType, attackSpeed, attackDistance, projectileSpeed, hasProjectile, isMonsterWeapon; KHÔNG có damage — server-driven), `StaticWeaponData` (load Resources + pool theo rarity bỏ vũ khí quái, GetData/GetRandom) đăng ký trong StaticGameData.Load(). Sinh **72 asset** ở `Resources/Scriptable Objects/Weapons` bằng `tools/gen_weapon_assets.py` (59 người chơi + 13 quái). WeaponData.cs.meta guid CỐ ĐỊNH `7d3f1c9e5a4b42c88e0f6b2a9c1d4e5f` (asset ref guid này). Nguồn data: sheet GDD tab "C1. Vũ khí".

Recheck vs data GỐC (2026-07-24): đối chiếu 72 asset sinh ra với `AssetRipper/ExportedProject/Assets/MonoBehaviour/*.asset` (ScriptableObject thật, script guid `9cd8bb47a301bac36ab257d0b4f87223`) bằng `tools/recheck_weapons.py` → **0 sai lệch** mọi field (ItemId/ItemName/Rarity/WeaponType/AttackSpeed/AttackDistance/ProjectileSpeed), `hasProjectile` khớp 39/39 với ProjectilePrefab được set, Localization=Items.Weapon.Name.{itemId} đúng 100%. Field gốc CHỦ ĐÍCH bỏ: `AreaDamageRadius` (=0 toàn bộ 72), và các reference (ProjectilePrefab/WeaponAnimator/WeaponSprite/HitAudios/ShootAudios) — layer data chỉ giữ stat. Đã verify LIVE trong Unity qua MCP execute_code: StaticWeaponData load 72, pool drop loại đúng 13 vũ khí quái, 0 lỗi console.

**C2–C9 Trang bị ✅ (data thật, 2026-07-24)** — tất cả ở `Scripts/Gears/`, đăng ký trong StaticGameData: `gears/capes/wings/forge`.
- **C2–C6 (Mũ/Găng/Nhẫn/Dây chuyền/Ba lô)**: 5 loại catalog cùng cấu trúc → GỘP 1 class `GearItemData:ScriptableObject` (fields slot/assetName/itemId/displayName/rarity/localizationKey/icon; lưu localizationKey TƯỜNG MINH vì Neckle→key "Necklace"), enum `GearSlotType`, loader `StaticGearItemData` (bucket theo slot + pool slot×rarity). **188 asset** ở `Resources/Scriptable Objects/Gears/{Helmets54,Gloves54,Rings30,Necklaces30,Backpacks20}` sinh bằng `tools/gen_gear_assets.py` (quét asset gốc theo script-guid từng loại để đủ cả boss). GearItemData.cs guid Unity tự sinh `db263ffe4d7dc7a40852fe2457c254f0`.
- **C7 Áo choàng**: `CapeData` (12 item, rarity 0–5, có healthBase/damageBase/scaler/subStatCount) + `CapeConfigData` (1 asset: maxLevel/summonCost/subStatCount + 6 CapeRarityConfig công thức XP) + `StaticCapeData`. Sinh bằng `tools/gen_cape_wing_assets.py`.
- **C8 Cánh**: `WingData` (10 item) đủ stat + kinh tế craft; `rerollCosts` giải mã từ hex LE int32 (`0300000006000000`→[3,6]); subStats list {type,value}; craftOreType/rerollOreType/levelUpOreType lưu INT gốc (chưa map enum — TODO khi làm E Mining/combat stat). `StaticWingData`. **Lưu ý sheet SAI**: reroll Wyrmling sheet ghi "3,6,2009", thực tế [3,6,9].
- **C9 Forge**: ma trận xác suất 100 cấp × 10 cột (tên cột theo forge: Common..Mythic,Divine,Celestial,Immortal,Eternal — KHÁC enum Rarity item, nên lưu POSITIONAL không ép enum). JSON ở `Resources/Json/forge_rarity_probabilities.txt` (đuôi **.txt** không phải .json — Spine importer nuốt .json báo lỗi), loader `StaticForgeData` (Newtonsoft). Nguồn: `DecodedData/tables/forge_rarity_probabilities.csv` (APK≡live).
- Recheck vs gốc bằng `tools/recheck_gears.py` + inline: **0 sai lệch mọi field** C2–C8; forge 100 dòng đều tổng=100. Verify LIVE Unity qua execute_code: gears 188, capes 12+config, wings 10, forge 100×10, **0 lỗi console**.
- **Icon còn thiếu 37** (data vẫn đúng, chỉ thiếu ảnh): 25 boss helmet/glove + 12 player Ring/Neckle tier 9–10. Nguyên nhân: PNG chưa crop lúc trước; tier 9–10 dùng atlas guid `26b0baaf...` chưa có trong map crop (`tools/upload_images_bc.py` ATLAS dict). Sprite+atlas CÓ sẵn (`Assets/Sprite/tier_09_ring_01.asset` rect + `Texture2D/...Immortal_Divine`). **User: để trống, xử lý sau.** Cape/Wing icon đã đủ 100%.

Icon vũ khí (2026-07-24): đã copy **61 PNG** (từ `DecodedData/_img_tmp_bc/<assetName>.png`) vào `_Assets/_ResourceGame/WeaponIcons/`, sinh .meta Sprite (template mirror `_ResourceGame/Avatar/angel.png.meta`: textureType 8, spriteMode 1, guid riêng + spriteID=guid[:24]+"00000000"), gán `icon: {fileID: 21300000, guid, type: 3}` vào từng WeaponData asset bằng `tools/attach_weapon_icons.py`. CHỈ gán cho asset mà data gốc CÓ Icon (61 = 59 người chơi + 2 Cultist). **11 boss/quái để icon NULL — ĐÚNG BẢN GỐC** (Dragon×4, Lych×3, Ogre×2, Witch×2): gốc field `Icon: {fileID: 0}` vì boss không rơi cho người chơi nên không cần icon inventory; boss vẫn CÓ `WeaponSprite` (sprite chiến đấu) — nếu sau muốn hiện boss trong bảng tra cứu/debug thì lấy WeaponSprite làm icon (PNG có sẵn ở `_img_tmp_bc`). **User đã QUYẾT: tạm để trống, chọn sau khi làm sâu.** → KHÔNG tự gán, chờ user yêu cầu.

**D1 Companion ✅ (data thật, 2026-07-25)** — module `Scripts/Companions/`: `enum CompanionType` (16 giá trị lấy đúng `CompanionType.cs` gốc: DPS0..Immortality15), tái dùng `Rarity`. `CompanionData:ScriptableObject` PHẲNG giữ TOÀN BỘ stat (di chuyển/timing/combat lõi + mọi nhóm effect Bomb/Burn/MultiSlow/ChainHeal/Lightning/Beam/Guardian/AoeSlow/Blaster/Meteor/Siphon/HealNova/Clone/Immortality — con nào không dùng thì =0), khoá = `assetName` (companion KHÔNG có itemId số; localizationKey=`Items.Companion.Name.{assetName}`). Bỏ đúng như weapon: mọi Prefab/ProjectilePrefab/BombPrefab/*ImpactPrefab/âm thanh+volume/màu VFX (LightningFlashColor/SlowFlashColor/CloneTintColor); chỉ giữ `icon`. `StaticCompanionData` (load Resources + pool theo rarity + GetData/GetByType/GetRandom) đăng ký `companions` trong StaticGameData.Load(). Sinh **18 asset** ở `Resources/Scriptable Objects/Companions` bằng `tools/gen_companion_assets.py` (đọc thẳng `DecodedData/tables/CompanionData.json`). CompanionData.cs.meta guid CỐ ĐỊNH `3f8b1d6a9c2e4f5b8a0d7c3e6f1b4a29`. Icon: copy 18 PNG từ `DecodedData/_img_tmp_d/` vào `_ResourceGame/CompanionIcons/` (đủ 18, không thiếu như gear). 18 con = 6 tier × 3 (rarity Common→Mythic, tier 6 asset tên "Divine" nhưng Rarity=Mythic). Recheck `tools/recheck_companions.py`: **0 sai lệch / 1386 field** vs JSON gốc. Verify LIVE Unity execute_code: load 18, pool 6×3, GetByType OK, **0 lỗi console**. Nguồn GDD: tab "D1. Companion".

**Prefab game-object ✅ (2026-07-25)** — copy **54 prefab** entity từ `AssetRipper/ExportedProject/Assets/GameObject` vào `Assets/_Assets/Prefabs/{Maps(5),Companions(18),Projectiles(25),Obstacles(6)}` với KHÔI PHỤC guid đầy đủ. Phát hiện then chốt: các prefab entity gần như KHÔNG có script tuỳ biến (chỉ 2 loại: CompanionGuardianEffect, SRDebugger) — 99% là component built-in (SpriteRenderer/collider/Rigidbody2D/ParticleSystem), và **sprite là standalone (sprite name == texture name), KHÔNG atlas** → khôi phục sạch. Pipeline (scratchpad session 6b027155): (1) `analyze.py` UnityPy đọc APK dump cây phụ thuộc; (2) `bridge.py` ghép cây YAML export ↔ cây APK theo GO-name → export-sprite-guid→tên (76 guid, 0 xung đột); (3) cả 76 texture ĐÃ có sẵn trong `_ResourceGame` (từ pass FX trước) → `copy_sprites.py` rewrite `m_Sprite` (guid export→resgame, type 2→3, 282 ref) + flip `pentagram` sang Sprite mode (spriteMode1/textureType8 giữ nguyên guid). Verify LIVE Unity: **54 prefab, 282 SpriteRenderer, 0 null sprite, 0 lỗi console**. Particle mat: glow1_ADD đã có (metas FX dùng EXPORT-guid nên companion resolve tự động); copy thêm 3 mat healing_AB/slash02_ADD/slash_circle02_ADD vào `Fx/Materials` với export-guid + rewrite tex→det_guid `_ResourceGame/Fx/{healing,slash02,magic_runecircle}` → 5/5 particle mat OK. **Còn thiếu chủ đích (đã báo user)**: 69 missing-script (Companion/Projectile/Map/Block controller — chưa rebuild, phần 4 Combat); 2 UI sprite + font trong BlockHPUIPrefab/InfoBlock (dùng UI Image/TMP, không phải SpriteRenderer); 2 particle mesh ETFX_CirclePlane(Half) của Blaster/Meteor (low-value). Prefab .meta guid = det_guid('prefab/{folder}/{name}').

**SỬA: Hero CÓ prefab** — `PreviewCharacter.prefab` (rig "Soldier": Animator+Rigidbody2D+CircleCollider2D → Body sprite=`body` + LeftHand/RightHand sprite=`hand` + slot trang bị RỖNG Weapon/Helmet/Backpack/Wings/WeaponPart1-4/ProjectilePart điền runtime theo gear + 3 Cloak Spine mesh + 22 particle buff effect). Copy vào `Prefabs/Hero/` (`hero_bridge.py`→`hero_maps.json`). Verify LIVE: 3/3 sprite body+hand OK (10 slot null = trang bị rỗng ĐÚNG THIẾT KẾ), **22/22 particle material OK, 0 lỗi console**. Recover thêm 3 particle mat vào Fx/Materials (export-guid): aura_slam_ADD(tex gradient_vertical_spiky), magic_runecircle_ADD(tex magic_runecircle), wave3_ADD(tex wave3) — rewrite tex→det_guid `_ResourceGame/Fx`. **Cloak Spine ✅ + Animator ✅ (2026-07-25)**: (a) 3 Cloak — remap script Spine `354902358/ebe353a9`→SkeletonAnimation cài `11500000/d247ba06193faa74d9335f5481b2b56c`, skeletonDataAsset export→project (short 9cbba933/mid 3347db1b/long 90f9b6ad), MeshRenderer material→project (short 3200f254/mid f4f874df/long 6e4e838b), clear MeshFilter mesh về fileID:0 (Spine tự gen). Verify: 3/3 SkeletonAnimation bound, 3/3 material OK. Spine assets ĐÃ đủ sẵn ở `_Assets/_Spine/cloak_*`. (b) Animator: copy `CharacterAnimatorController.controller`(guid GIỮ export 474eb790...,3 state Idle/Attack/Die + param IsIdle/IsAttacking/IsDying) + `PunchAnimatorController.overrideController`(guid 10473125c8fd7e544b3558d9e199f9b3 = ref của Soldier Animator) + 3 clip PunchIdle/PunchAttack/DieAnimation (guid GIỮ export 4c845c39/369383a6/a730242c, clip KHÔNG ref sprite ngoài) vào `Prefabs/Hero/Animations/`. Verify LIVE: Soldier→PunchAnimatorController, 3 clip nạp đúng, **0 lỗi console** (trừ warning benign BossRushMap missing-script). **Hero còn 4 missing-script cố hữu**: PreviewCharacter(root)/Soldier/EnchantmentEffects×2 — script gameplay chưa rebuild (như 69 cái kia). Enemies VẪN không có rig riêng — quái tái dùng rig Soldier lúc runtime, đổi body sprite.

Trick compile-check dotnet (đã kiện toàn): csproj netstandard2.1 tham chiếu **tất cả** `UnityEngine\UnityEngine.*Module.dll` (KHÔNG thêm facade `UnityEngine.dll` — gây CS0433 trùng type) + Newtonsoft AOT dll; HintPath phải path Windows `E:\...` (path MSYS `/e/...` MSBuild không hiểu). Script sinh csproj ổn định để ở scratchpad.

Khác StickIdle chủ đích: bỏ Firebase, `LoadModule<T>` generic thay ~30 hàm LoadXxx, thời gian qua `GameUtils.GetTimeNow()` (1 chỗ để thay server time), event dispatch để TODO chờ phần 6.

**Map + sinh enemy ✅ (procedural, 2026-07-26)** — module `Scripts/GameModes/Map/` (bám phát hiện [[dungonrush-map-spawn-procedural]]: DungeonRush KHÔNG có data layout, map sinh procedural). File: (1) enums thêm vào `Common/GameEnums.cs`: `MapEnvironmentType`(0..6), `DungeonType`, `MapCellType`. (2) `StaticMapData` — 7 MapConfig dựng lại từ gốc (grid 9×12, spawnObstacles/camera offset), hằng sinh MIN/MAX_OBSTACLE=10/15, MAX_GENERATION_ATTEMPTS=10; đăng ký `map` trong StaticGameData.Load(). (3) `MapGenerator` (static, pure) — tái tạo LevelGenerator: random box→BFS liên thông spawn→cửa→retry, deterministic theo seed, KeepClearZone struct. (4) `EnemySpawnGenerator` — scaling PLACEHOLDER neo trên hằng thật (army_power_level_scaler=20, exp=√10); màn cuối chương=boss. (5) `CampaignLevelBuilder.Build(stageId)` = entry ghép map+enemy, seed từ stageId. Test: ContextMenu "Test/Generate Current Campaign Level" trong GameDataTester (in ASCII map + enemy). **Verify chạy thật** bằng harness console scratchpad (shim Vector2Int/Mathf, include file nguồn thật): 6/6 seed liên thông, deterministic OK, box∈[10,15]. Compile-check dotnet OK (đã thêm 4 Compile Include vào Assembly-CSharp.csproj — Unity sẽ regenerate). Tài liệu RE: `DecodedData/MAP_AND_SPAWN_MODEL.md`.

**Phần 4 Combat — grid+spawn runtime ✅ (2026-07-26)** — module `Scripts/GameModes/Combat/`: (1) `GridSpace` struct — ô↔world (CellToWorld/WorldToCell, canh giữa X, mirror CellSize/GridOrigin gốc). (2) `EnemyUnit:MonoBehaviour` — giữ stat runtime, Init(EnemySpawnInfo). (3) `LevelRuntimeBuilder:MonoBehaviour` — entry runtime: gọi CampaignLevelBuilder → Instantiate obstacle/enemy/player-spawn theo GridSpace, gom vào con `_Level`, ContextMenu Rebuild/Clear, OnDrawGizmosSelected vẽ lưới+cửa. **Chạy được ngay trong scene trống**: chưa gán prefab thì tự tạo placeholder (Cube=box nâu, Sphere=enemy đỏ/boss to, Quad=player xanh). Fields Inspector: overrideStageId(0=curStageId), environment, cellSize, 3 prefab optional. **Verify LIVE Unity (execute_code)**: stage101→13 obstacle+3 enemy+16 playerSpawn(8×2), enemy lv1 hp200 atk20; stage110(cuối chương)→1 boss lv10 hp20146 atk2015 (×6+segment√10); door(4,11); determinism OK; cleanup sạch; **0 error/warning console**. Compile dotnet OK (+3 Compile Include csproj). Battle mechanic thật (di chuyển/đánh nhau/HP UI/loot) CHƯA làm — đây mới là khung dựng grid+spawn. **Gán art thật + preview (2026-07-26)**: thêm field `mapPrefab` (SpawnEnvironment đặt nền ở tâm lưới) + boss scale ×1.6 ở nhánh prefab thật; placeholder đổi sang `MaterialPropertyBlock` (hết warning material-leak edit-mode). Scene `Assets/_Assets/Scenes/_MapPreview.unity` — LevelRuntimeBuilder gán MainMapPrefab+BlockPrefab+PreviewCharacter, buildOnStart=true (mở scene→Play là thấy; hoặc chuột phải component→Rebuild). Verify LIVE screenshot: nền grid+cửa, 13 box BlockPrefab, 3 Soldier ở cổng (Soldier chưa mặc gear nên nhỏ, đúng thiết kế slot rỗng), boss to hơn; **0 error/warning console**.

**⚠️ Phần 4b Battle loop kiểu SNAKE — ĐÃ REVERT (SAI THỂ LOẠI)**: DungeonRush là RPG ACTION không phải game rắn (xem [[dungonrush-genre-rpg-action]]). User đã tự revert toàn bộ battle loop snake bên dưới + các accessor thêm vào LevelRuntimeBuilder. GIỮ LẠI phần grid+spawn (MapGenerator/EnemySpawnGenerator/GridSpace/EnemyUnit/LevelRuntimeBuilder bản gốc). Nội dung mô tả dưới đây chỉ để tham khảo bug-import, KHÔNG phải hướng combat đúng — chờ user mô tả cơ chế RPG action.

~~**Phần 4b Battle loop (vertical-slice, 2026-07-26 — ĐÃ REVERT)**~~ — thêm 6 file `Scripts/GameModes/Combat/`: `CombatPlaceholder` (tạo primitive+MaterialPropertyBlock), `BoxObstacle` (hp/goldReward), `LootPickup` (snake tới gần→items.Receive), `DoorController` (đóng đỏ/mở xanh), `SnakeController` (đầu đi tới đích + thân trail theo cung độ dài + HP→số đốt, SetDestination/HaltForCombat/TakeDamage), `BattleController` (`[RequireComponent(LevelRuntimeBuilder)]` orchestrator: build màn→gắn BoxObstacle cho obstacle→spawn snake giữa hàng dưới + cửa→loop: snake nhắm enemy gần nhất, box chắn trước mặt thì halt+đập, trao sát thương 2 chiều, chết→DropLoot; dọn sạch enemy→door.Open; chạm cửa mở→NextStage(campaign.PassStage); snake chết→Retry; HUD OnGUI). LevelRuntimeBuilder thêm accessor SpawnedObstacles/SpawnedEnemies/Grid/DoorWorld/Container + track list. **Auto-balance army**: dps=(totalEnemyHp+boxCost/2)/clearSec(6s); hp=totalEnemyAtk×clearSec×**1.6** (biên an toàn — fix bug boss chết trước khi hạ; hệ số 0.9 ban đầu thua). **Verify bằng simulation C# standalone** (Unity MCP đang NGẮT nên chưa play-mode): shim Unity + MapGenerator thật + mô phỏng đúng tick BattleController → stage thường WIN 9.3s minHp490/576 (3 enemy+3 box, gold45 gem9); boss WIN 8.9s minHp7355/19344 (gem25). Compile dotnet OK (+6 Compile Include). **VERIFY LIVE PLAY-MODE ✅ (2026-07-26)**: scene `Assets/_Assets/Scenes/_BattlePreview.unity` (GO "Battle" = BattleController+LevelRuntimeBuilder gán MainMap/Block/Hero, camera ortho 7.5 @ (0,5.5,-10)). Vào Play qua MCP: snake (chuỗi cầu xanh, thân bám trail) bò lên diệt enemy→gem, phá box→gold, dọn sạch→cửa mở (xanh)→về cửa→qua màn TỰ ĐỘNG (chạy một mạch 101→102→103→…→105 trong vài giây), gold/gem tích luỹ (135/36), auto-balance máu tăng theo màn (576→1939→3626). HUD OnGUI đúng. **0 lỗi script** (chỉ warning FMOD no-audio-device vô hại).

**BÀI HỌC bug import (2026-07-26)**: file .cs tạo bằng Write tool LÚC UnityMCP NGẮT → Unity sinh `.meta` CỤT (chỉ fileFormatVersion+guid, THIẾU block MonoImporter + newline) → file bị LOẠI khỏi sourceFiles của Assembly-CSharp (dù importer=MonoImporter, MonoScript hợp lệ) → lỗi CS0246 "type not found" ở file KHÁC dùng nó, dù dotnet compile sạch. ForceUpdate reimport + xoá meta + RequestScriptCompilation(CleanBuildCache) ĐỀU KHÔNG sửa được. **Cách sửa dứt điểm**: `AssetDatabase.DeleteAsset` (safety_checks=false) 2 file kẹt (DoorController.cs, LootPickup.cs) rồi Write tạo lại mới → refresh → vào assembly OK. Chẩn đoán bằng `CompilationPipeline.GetAssemblies().sourceFiles` để xem file có trong assembly không.

**Phần 1 Scene flow ✅ (2026-07-27)** — bản RÚT GỌN (bỏ Firebase/RemoteConfig/UIManager/GameConfig của StickIdle). 3 script: `Scripts/Login/Root.cs` (bootstrap: targetFrameRate60+runInBackground+noSleep→LoadScene Login), `Scripts/Login/Login.cs` (coroutine SetLoading→`GameData.Init()`→ẩn loading+bật nút TapToPlay; version=`Application.version`; click→`GameData.Save(true)`→LoadScene Lobby; fields btTapToPlay/txLoading/txVersion mirror StickIdle), `Scripts/Lobby/LobbyManager.cs` (hiện txWelcome=userName + txGold, guard tự Init nếu mở thẳng Lobby). 3 scene tạo+wire+lưu bằng execute_code (Canvas ScaleWithScreenSize 1080×1920 + EventSystem, camera nền tối); Build Settings Root(0)/Login(1)/Lobby(2). Verify LIVE play-mode: Root→Login tự nhảy, data load (userName Player######), loading ẩn, nút bật, click→Lobby, **0 lỗi console**. Thêm `Scripts/_Test/DebugSceneSwitcher.cs` — phím tắt debug GIỮ C + 1/2/3 → Root/Login/Lobby (như cheat StickIdle); tự sinh GameObject bền `[RuntimeInitializeOnLoadMethod]`+DontDestroyOnLoad (không cần wire scene), chỉ `#if UNITY_EDITOR||DEVELOPMENT_BUILD`.

**INPUT: dùng Input Manager CŨ (2026-07-27, user chọn "Old only" như StickIdle)** — `ProjectSettings.asset` `activeInputHandler: 0` (0=Old,1=New,2=Both). DebugSceneSwitcher dùng `Input.GetKey(KeyCode.C)`+`Input.GetKeyDown(KeyCode.Alpha1/2/3)`. EventSystem 2 scene Login/Lobby swap `InputSystemUIInputModule`→`StandaloneInputModule` (guid ugui package `4f231c4fb786f3946a6b90b886c48677`, fileID 11500000, m_EditorClassIdentifier `UnityEngine.UI::UnityEngine.EventSystems.StandaloneInputModule`; 10 field default m_HorizontalAxis..m_ForceModuleActive). **BÀI HỌC**: Input cũ (`Input.GetKey`) NÉM InvalidOperationException khi activeInputHandler=1(New); đổi activeInputHandler BẮT BUỘC restart Unity. Bước cuối làm lúc UnityMCP NGẮT → hand-edit trực tiếp 3 file (ProjectSettings + 2 scene) — CHỈ an toàn khi Unity ĐÃ ĐÓNG (verify: 0 process Unity.exe + `Temp/UnityLockfile` rename-test unlocked); Unity đang mở sẽ ghi đè. **CHƯA verify play-mode sau đổi** (chờ user mở lại Unity).

**Phần 6 Patterns core + Effects ✅ (2026-07-27)** — port từ StickIdle, compile dotnet 0 lỗi. (a) **Design Patterns** `Scripts/GameDesignPatterns/`: `Singleton/Singleton.cs`, `Observer/EventDispatcher.cs` (nguyên bản) + `Observer/EventID.cs` **RÚT GỌN** còn `None`+`ResetMode` (bỏ ~100 event riêng của Stick, thêm dần sau), `ObjectPooling/ObjectPooling.cs` (nguyên bản) + `ObjectPooling/PoolingController.cs` **RÚT GỌN** chỉ region Fx+TextDamage (bỏ Bullets/Enemies/GoldDrop vì chưa có BaseBullet/BaseEnemy/GoldDrop/PrefabUtils). Giữ `.meta` GUID gốc của Stick để tái dùng prefab sau. (b) **Effects** `Scripts/Effects/` (theo cấu trúc Stick): `BaseFx.cs`, `FxController.cs`, `CC/{FxHex,FxRooted}.cs`, `TextDamage/{CombatText,TextDamageAnimation}.cs`. Spine+DOTween đã có sẵn trong project nên FxRooted giữ nguyên. (c) **Bổ sung file có sẵn**: `Utilities/Yielder.cs` (mới, nguyên bản), `Utilities/Extensions.cs` +`StartDelayAction`+`ToLetter`, `Utilities/DebugCustom.cs` +`Assert`+`LogWarning(Object,format,args)`, `Common/StaticValue.cs` +`SORTING_LAYER_UI="UI"`, `Common/GameEnums.cs` +enum `TextDamageStatus`. (d) **3 điểm ADAPT** (đánh dấu `TODO(follow-stick)` trong code): FxController.ShowFxDie đổi `BaseEnemy`→`EnemyUnit` (isBoss/transform.position); ShowStatus bỏ tạm LocalizeManager; CombatText.Initialize dùng `Camera.main` thay CameraController. **CHƯA verify LIVE Unity** (chỉ compile-check dotnet, +12 Compile Include csproj — Unity regenerate).

**Localize ✅ + Combat foundation (BaseUnit + BattleMechanic) ✅ (2026-07-27)** — port từ StickIdle, compile dotnet **0 lỗi** (đã +file vào Assembly-CSharp.csproj — Unity regenerate). **CHƯA verify LIVE Unity.**
- **Localize** `Scripts/Localize/` (giữ .meta GUID gốc + prefab): LocalizeManager(Singleton, bỏ Firebase→DebugCustom, guard `using UnityEditor`), LocalizeText, LanguageID(15), LocalizeEditorUtils(#if UNITY_EDITOR). Data JSON copy vào `Resources/Json/Localize/{Languages(15),Categories(4)}`. Thêm `EventID.ChangeLanguage`.
- **Combat foundation "gọn + adapt"** (user chọn hướng này thay vì clone full ~20k dòng StickIdle): closure của BaseUnit thực chất là CẢ runtime combat StickIdle (~8.5k dòng lớp đầu, phụ thuộc vòng tròn với BattleMechanic + GameController/UIGamePlay/UIManager/AudioManager/AnimationController/HealthBar/CameraController/BaseBullet — gần như tất cả MISSING ở DungeonRush; chỉ FxController/BaseFx/Yielder/DebugCustom có sẵn). Giải pháp: **port thật** phần self-contained + **slim/stub** các manager.
  - Port thật: `Stats/`(Stats,StatModifier[bỏ UI-string ext+Firebase],StatUtils[bỏ HandNavigationType],BaseStats), `Utilities/VectorUtils.cs`, `BattleMechanic/`(13 file — bỏ enum `TextDamageStatus` trùng GameEnums; StatAdjustmentData.IsStackable bỏ nhánh BaseHero), `Unit/BaseUnit.cs`(1654 dòng — adapt: bỏ check UIGamePlay/UIManager trong sfx, `PostEvent`→extension `this.PostEvent`, `DebugCustom.ShowLog`→`Log`).
  - Slim/stub (đánh dấu `TODO(follow-stick)`, đều ở `Scripts/Unit/`): AnimationController(thay Spine 533 dòng, no-op), HealthBar, CameraController, AudioManager, BaseBullet, GameController(+CombatMode/CombatMap, ClampPointInMap=identity).
  - Thêm `StaticValue.TAG_TEAM_A/B`("TeamA"/"TeamB" — cần khai tag trong Unity), `EventID.EndGame/UnitDie`, `Extensions.GetRandomPointAround`.
  - **Bước tiếp**: cho `EnemyUnit`(GameModes/Combat, hiện data-holder 24 dòng) kế thừa BaseUnit; AnimationController Spine thật; nối GameController vào spawn procedural; verify LIVE Unity.

**Mode base ✅ (core base, 2026-07-29)** — module `Scripts/GamePlay/Mode/` (user đã đổi tên GameModes→GamePlay/Mode): dựng khung quản lý mode chơi kiểu StickIdle (GameModes/BaseMode+UIModeBase) nhưng ADAPT cho DungeonRush RPG-action lưới, BỎ hết phần side-scroller của Stick (ActionNextWave/DOMoveX/nextWavePositionX/spawn-ahead — sai thể loại). (a) `BaseMode:MonoBehaviour` — khung vòng đời `Initialize→LoadEnemyFiles→PlayMusic→CreateMap→CreateTeamA→CreateTeamB→InitModeDone→StartGame`, trạng thái isEndMode/isPause/isWin/flagBattleTimer/battleTimer, EndGame(bool)+Reset+Pause/Resume, RoutineTimer đếm ngược (KHÔNG wave), OnUnitDie→OnAllyDie(hết teamA→thua)/OnEnemyDie→CheckRemainingEnemies(hết teamB→thắng), cheat Editor W=win/L=lose. CreateMap/CreateTeamA/CreateTeamB để TRỐNG (virtual) cho mode con điền. (b) `UIModeBase:MonoBehaviour` — lược `ClockTimer` (chưa có type). **Quyết định thiết kế** (user chọn): (1) phạm vi bước này = CHỈ base, chưa đụng CombatDirector; (2) hướng đích = mode con HẤP THỤ logic Build/spawn của CombatDirector (CombatDirector thành thừa) — làm ở bước sau khi dựng CampaignMode/DefaultLevel cụ thể. **Naming**: GameController.mode ở DungeonRush = CombatMode (state trận teamA/teamB/map, BaseUnit đọc 6 chỗ), KHÁC Stick (mode==BaseMode); BaseMode chỉ ĐỌC qua GameController.Instance.mode, KHÔNG chiếm chỗ → tránh ripple BaseUnit. AudioManager slim chỉ có PlaySfx nên PlayMusic() để no-op TODO. Compile dotnet Assembly-CSharp.csproj **0 lỗi** (+1 Compile Include UIModeBase.cs; BaseMode.cs Unity đã liệt). CHƯA verify LIVE Unity.

**CampaignMode ✅ (hấp thụ CombatDirector, 2026-07-29)** — `Scripts/GamePlay/Mode/Campaign/CampaignMode.cs` (`CampaignMode:BaseMode`): port TOÀN BỘ logic dựng màn của CombatDirector cũ vào hook vòng đời BaseMode — `CreateMap`(EnsureGameDataLoaded→CampaignLevelBuilder.Build(stageId,type)→GridSpace→CombatMap.Setup wall→SpawnEnvironment/SpawnObstacles), `CreateTeamA`(SpawnHeroAndPets, hero giữa hàng dưới + pet rải quanh), `CreateTeamB`(SpawnEnemies, boss scale ×1.6). `Build()` [ContextMenu Rebuild] = Reset()+Initialize(); `Reset()` [ContextMenu Clear] = ResetBattle+destroy container+isEndMode=false. Giữ nguyên field Inspector cũ (overrideStageId/buildOnStart/cellSize/3 prefab/chỉ số hero-pet placeholder). type=DefaultLevel set ở Awake. Nhờ kế thừa BaseMode: tự có RoutineTimer/EndGame/Pause/Resume/OnUnitDie→thắng-thua/cheat W-L. **CombatDirector.cs + .meta ĐÃ XÓA** (orphan: guid `23536443e840ea6418e14dc44f960723` không ref ở scene/prefab/code nào, chỉ comment) — csproj đổi Compile Include CombatDirector→CampaignMode. README cập nhật (entry chạy trận = CampaignMode). Compile dotnet **0 lỗi**. SpawnInBattle→Active→AddUnit tự nạp teamA/teamB nên StartGame.remainingEnemies=teamB.Count đúng. **CHƯA verify LIVE Unity** (chưa gán CampaignMode vào scene + prefab unit; scene _BattlePreview/_MapPreview vẫn dùng builder cũ).

**LIVE test CampaignMode ở _BattlePreview (2026-07-30) — CHẶN bởi rig mismatch**: đã wire GO "CampaignMode" vào scene `_BattlePreview` (mapPrefab=MainMapPrefab, obstaclePrefab=BlockPrefab, hero/pet/enemyPrefab=PreviewCharacter, stage101, buildOnStart) qua MCP execute_code; tag TeamA/TeamB CÓ sẵn. Vào Play: **CampaignMode chạy ĐÚNG vòng đời** (Start→Build→Initialize→CreateMap dựng map OK→CreateTeamA→SpawnUnit) NHƯNG **NRE ở BaseUnit** khi spawn unit. **NGUYÊN NHÂN GỐC (blocker lớn)**: BaseUnit.Awake (port từ StickIdle) đòi rig: root có Rigidbody2D+AudioSource+AnimationController, child trực tiếp "Body" có Collider2D, node "FlipPoints">"CenterBody"/"FirePoint"/"health-bar"(HealthBar). Prefab thật DungeonRush (`Prefabs/Hero/PreviewCharacter.prefab` = rig "Soldier" gốc rip) CẤU TRÚC KHÁC HẲN: root chỉ Transform → child "Soldier"[Animator,SortingGroup,Rigidbody2D,CircleCollider2D] → child "Body"[chỉ SpriteRenderer]; KHÔNG có FlipPoints/CenterBody/FirePoint/health-bar/AudioSource, collider ở Soldier không ở Body. Grep toàn project: **0 prefab có "FlipPoints"/"health-bar"** → không có prefab combat-ready nào. Vậy BaseUnit (StickIdle) KHÔNG tương thích art rig thật của DungeonRush. **2 hướng chờ user chọn**: (A) ADAPT BaseUnit.Awake + phần phụ thuộc (ActiveHpAndCollider/hpBar/firePoint/AnimationController) sang rig Soldier thật (đụng combat core); (B) DỰNG prefab combat-ready (thêm FlipPoints/CenterBody/FirePoint/health-bar + collider ở "Body" + AudioSource + AnimationController) khớp contract BaseUnit (giữ core, fiddly). GO "CampaignMode" trong scene CHƯA save (revert khi đóng Unity nếu không lưu). CombatDirector cũ (đã xóa) cũng CHƯA từng verify — cùng gap này.

Còn lại: **QUYẾT rig mismatch (A/B) để combat/mode chạy LIVE**; UIMode con (UICampaign) + nối UI timer/win-lose; 3 GameConfig+flags, 4c polish combat (army power từ gear/companion thật thay auto-balance; enemy AI di chuyển; animation/hiệu ứng), 5 UI (UIManager/BaseUI). Patterns core (Singleton/EventDispatcher/Pooling) ✅ — các manager combat mới ở dạng slim/stub, thay bản đầy đủ khi làm combat sâu.

Trick verify không cần mở Unity: dotnet build csproj tạm reference `UnityEngine.CoreModule.dll` + Newtonsoft từ PackageCache.

MCP Unity (giống StickIdle): package `com.coplaydev.unity-mcp` (git CoplayDev/unity-mcp?path=/MCPForUnity#main) trong manifest project con + server `UnityMCP` type http `http://127.0.0.1:8080/mcp` đăng ký trong `.claude.json` tại project key dấu `/` = `E:/00Work/00Project/2026DungOnRush` (key Claude tự tạo, key double-backslash là rác cũ). Cần: mở Unity cho cài package + bật MCP bridge (Window > MCP For Unity), rồi restart Claude Code để nạp server. Backup claude.json: `~/.claude.json.bak-*`. Lưu ý: Claude session chạy từ thư mục cha `2026DungOnRush`, Unity project ở con `2026DungeonRushUnity`.
